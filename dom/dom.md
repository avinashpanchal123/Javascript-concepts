  - DOM is creatd by web browser
  - The DOM represents the document as a hierarchical tree structure,
    where each node is an object representing a part of the document.
  - The DOM allows us to interact with and manipulate web pages dynamically.  